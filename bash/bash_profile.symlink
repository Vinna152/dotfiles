# Link bashrc so that Mac and Linux uses the same bash config.
if [ -f ~/.bashrc ]; then
   . ~/.bashrc
fi


# added by Anaconda2 5.0.1 installer
export PATH="/Users/arneschreuder/anaconda2/bin:$PATH"
